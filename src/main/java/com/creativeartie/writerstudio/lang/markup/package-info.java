/**
 * Parse text to a {@link Document} with {@link StyleInfo}.
 *
 * Package class relations:
 * <img src="doc-files/classes.png" />
 *
 * It should be mention that the span stores a list {@link Span} instead of
 * specific subclasses, but the linked {@link Span} are made for that classes.
 */
package com.creativeartie.writerstudio.lang.markup;
